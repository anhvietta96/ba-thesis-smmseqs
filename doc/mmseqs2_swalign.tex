In this stage, for all filtered query-target sequence pairs an exact, unbounded Smith-Waterman align-
ments with affine gap penalties is computed. This module also extensively utilizes SIMD vectorization.